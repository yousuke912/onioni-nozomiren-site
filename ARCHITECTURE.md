# 実装構成

## 結論

Next.js App Routerの静的出力を、Cloudflare Workers Static Assetsへ配置する構成を採用します。

## 代替案と比較

| 案 | 特徴 | 今回の適合度 |
|---|---|---|
| Next.js静的出力 + Workers Static Assets | 実行サーバー不要。速く、安く、壊れにくい | 採用 |
| Next.js + vinext + Workers | SSRやサーバー処理に対応。現時点ではベータ | 今回は不要 |
| Next.js + OpenNext + Workers | SSRやサーバー処理に対応。設定と運用が増える | 今回は過剰 |

## 選定根拠

- 今回は公式情報を見せ、LINE・SNSへつなぐサイトで、ログイン・DB・入力フォームがない。
- 写真と文章はビルド時に固定でき、静的出力が最も高速で安定する。
- Cloudflare Workersの静的アセット配信を使うため、固定インフラ方針から外れない。
- 将来、申込フォームや会員機能が必要になった場合は、機能を追加する前に構成を再検討する。

## 実装手順

1. `npm install`
2. `npm run dev` で内容を確認
3. `npm run lint` と `npm run build` を実行
4. 公開前チェックを完了
5. Cloudflare側の公開・認証方針を確定
6. `npm run deploy` を実行

## 非機能要件への影響

- セキュリティ: 入力フォーム・API・秘密情報がなく、攻撃面が小さい。外部リンクには安全属性を付与。
- パフォーマンス: 静的配信、画像の遅延読込、外部動画の直接埋め込み回避で表示を軽量化。
- 運用性: 文章・写真・色の編集場所を限定。ビルドで不整合を検出できる。
- アクセシビリティ: 見出し構造、スキップリンク、キーボードフォーカス、動きを減らす設定、スマホの大きなタップ領域を実装。
